{
	"name": "game-database",
	"version": "1.0.0",
	"description": "game database",
	"type": "module",
	"main": "bin/index.js",
	"scripts": {
		"start": "concurrently \"npm run server\" \"npm run cli-start\"",
		"dev": "concurrently \"nodemon --experimental-json-modules server.js\" \"SET PORT=3000 && nodemon src/index.js\"",
		"server": "node --experimental-json-modules server.js",
		"cli-start": "set PORT=3000 && node src/index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Tom Chestnut",
	"license": "MIT",
	"bin": {
		"game-database": "src/index.js"
	},
	"dependencies": {
		"express": "^4.17.1"
	},
	"devDependencies": {
		"concurrently": "^6.5.1",
		"nodemon": "^2.0.15"
	}
}
